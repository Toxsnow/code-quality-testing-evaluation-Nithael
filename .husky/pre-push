#!/bin/sh
# Run the monorepo test script. Using a POSIX shell wrapper avoids shebang
# interpretation issues on some Git installations while still invoking npm
# (via npx to ensure a local resolution if needed).
npx --no -- npm run test
